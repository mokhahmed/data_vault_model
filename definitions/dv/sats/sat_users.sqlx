
config {
  type: "incremental",
  schema: "dvm",
  name: "sat_users",
  columns: dv_models.users_model_doc,
  description: "satellite users table",
  uniqueKey: ["user_hash_id", "hash_diff"],
  tags: ["datavault"]
}

${
    dvm.get_sat(
        dv_models.users_model,
        ref("src_users"),
        ref("hub_users"),
        "src_users",
        "user_hash_id"
    )
}


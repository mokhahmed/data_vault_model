
config {
  type: "table",
  schema: "dvm",
  name: "sat_trips",
  columns: dv_models.trips_model_doc,
  description: "satellite trips table",
  uniqueKey: ["trip_hash_id", "hash_diff"],
  tags: ["data-vault"]
}

${
    dvm.get_sat(dv_models.trips_model, ref("src_trips"), ref("hub_trips"), "trip_hash_id")
}


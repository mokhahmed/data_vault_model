
config {
  type: "table",
  schema: "dvm",
  name: "hub_users",
  columns: dv_models.users_model_doc,
  description: "hub users table",
  uniqueKey: ["user_hash_id"],
  tags: ["data-vault"]
}

${
    dvm.get_hub(dv_models.users_model, ref("src_users"), "user_hash_id")
}
